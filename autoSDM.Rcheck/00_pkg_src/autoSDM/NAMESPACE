# Manually Managed NAMESPACE
# Core Dependencies
import(rgee)
import(reticulate)
import(jsonlite)
import(sf)

# Selective Imports to avoid conflicts (e.g., terra::na.omit)
importFrom(stats, setNames, runif, complete.cases, na.omit)
importFrom(utils, read.csv)
importFrom(magrittr, "%>%")
importFrom(data.table, as.data.table)
importFrom(stars, st_as_stars)
importFrom(parallel, mclapply, detectCores)

# Export Public API Only
export(generate_map)
export(evaluate_models)
export(format_data)
export(predict_at_coords)
export(install_autoSDM)
export(ee_hard_reset)
export(calculate_cbi)
export(calculate_classifier_metrics)
