% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoSDM.R
\name{evaluate_models}
\alias{evaluate_models}
\title{Evaluate SDM Models}
\usage{
evaluate_models(
  data,
  predict_coords,
  methods = NULL,
  scale = 10,
  output_dir = getwd(),
  aoi_year = NULL,
  count = NULL,
  n_trees = 100L,
  min_leaf_population = 5L,
  bag_fraction = 0.5,
  shrinkage = 0.005,
  max_nodes = NULL,
  variables_per_split = NULL,
  lambda_ = 0,
  polynomial = 2L,
  gee_project = NULL,
  python_path = NULL,
  options = list()
)
}
\arguments{
\item{data}{A data frame formatted via `format_data()`. Must have (longitude, latitude, year, present).}

\item{predict_coords}{Data frame of coordinates to evaluate at. Must have (longitude, latitude, year, present).}

\item{methods}{Character vector of method names.}

\item{scale}{Resolution in meters for sampling. Defaults to 10.}

\item{output_dir}{Directory to save results.}

\item{count}{Number of background points for training (if data is presence-only).}

\item{gee_project}{Optional. Google Cloud Project ID.}

\item{python_path}{Optional. Path to Python executable.}

\item{options}{Named list of advanced hyperparameters.}
}
\description{
Trains models on provided data and evaluates them at specific coordinates to calculate accuracy metrics.
}
