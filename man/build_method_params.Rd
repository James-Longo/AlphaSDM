% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AlphaSDM.R
\name{build_method_params}
\alias{build_method_params}
\title{Build the per-method hyperparameter list}
\usage{
build_method_params(
  methods,
  n_trees,
  min_leaf_population,
  bag_fraction,
  shrinkage,
  max_nodes,
  variables_per_split,
  svm_type,
  svm_kernel,
  svm_cost,
  svm_gamma,
  maxent_beta,
  maxent_features,
  knn_k,
  knn_search_method,
  knn_metric
)
}
\description{
Shared by [evaluate_models()] and [generate_map()] so that the model you
cross-validate is the model you map. Each method starts from the shared tree
arguments and then takes its own overrides.
}
\details{
Every override is conditional on the corresponding argument still holding its
default, so an explicit value from the caller always wins.
}
\keyword{internal}
