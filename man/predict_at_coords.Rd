% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_at_coords.R
\name{predict_at_coords}
\alias{predict_at_coords}
\title{Score coordinates with fitted AlphaSDM models}
\usage{
predict_at_coords(
  df,
  models,
  scale = NULL,
  aoi_year = NULL,
  gee_project = NULL,
  options = list()
)
}
\arguments{
\item{df}{Data frame with columns `longitude`, `latitude` and `year`.}

\item{models}{Named list of fitted model objects, as returned in the `models`
element of the internal training step (each carries `method`, `is_classifier`
and either a trained GEE classifier or centroid `weights`). Names are used to
label the output columns.}

\item{scale}{Embedding resolution in metres.}

\item{aoi_year}{Year of the Alpha Earth mosaic to sample.}

\item{gee_project}{Optional Earth Engine project override (normally set via [setup_gee()]).}

\item{options}{Named list of advanced options; `batch_size` sets the number of
points per download batch.}
}
\value{
A list with `data` (the input data frame plus a `similarity` column, or
  `similarity_<model>` columns when several models are supplied) and `metrics`.
}
\description{
Samples the Alpha Earth embeddings at a set of coordinates and scores them with
one or more already-fitted models, entirely server-side. Results are pulled back
in parallel CSV batches. Use [evaluate_models()] when you also want cross-validation
or accuracy metrics; this function is the lighter path for scoring new points with
models you already have in hand.
}
