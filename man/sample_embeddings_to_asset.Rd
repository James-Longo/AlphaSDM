% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gee_async.R
\name{sample_embeddings_to_asset}
\alias{sample_embeddings_to_asset}
\title{Sample 64-band embeddings at many points and materialise them to a GEE asset (chunked)}
\usage{
sample_embeddings_to_asset(
  df,
  scale,
  properties,
  years,
  project = NULL,
  chunk_size = 4000L,
  poll_seconds = 15,
  max_minutes = 60
)
}
\arguments{
\item{df}{data frame with longitude, latitude, year (+ any `properties`).}

\item{scale}{sampling scale (m).}

\item{properties}{point properties to carry through (besides the A00-A63 bands).}

\item{years}{list of years to sample (passed to `get_embeddings_at_fc`).}

\item{project}{GEE project id for the temp asset folder.}

\item{chunk_size}{points per chunk/export task.}
}
\description{
Sampling + exporting a very large or continent-wide point set in one task blows
GEE's memory. This samples and exports in independent chunks (each light enough to
fit), starts the chunk exports concurrently, awaits them, then merges the per-chunk
assets into ONE materialised FeatureCollection. Downstream training/classification
then read pre-computed embeddings (no re-sampling), which is what makes heavy,
many-class jobs tractable. Embeddings never leave GEE. Returns the merged
`ee$FeatureCollection` and the `asset_ids` to clean up with `ee_delete_asset_quietly`.
}
\keyword{internal}
