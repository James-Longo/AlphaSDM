#' Shared internal helper for live-updating timer
.run_command_with_timer <- function(label, active = FALSE) {
  # Placeholder for R-native timer if needed
  if (active) message("[autoSDM] ", label, "...")
}
